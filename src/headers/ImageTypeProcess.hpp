#ifndef ImageTypeProcess_hpp
#define ImageTypeProcess_hpp

namespace ImageTypeProcess {
    const char* findImageType(const char* imageFilename);
    int imageTypeRedirection(const char* fileType);
}

#endif
